language: java
branches:
  only:
    - master
    - Milestone-5
    - Milestone-6
	- Milestone-6-UI
notifications:
  email:
    - atumohan@umail.iu.edu
    - anujbhan@umail.iu.edu
    - arpiagar@iu.edu
jdk:
  - oraclejdk8
sudo: false
install: true
script: cd "ZenUI" && mvn clean install -DskipTests=true

before_deploy:
  - cd ..
  - zip -r ZenUI.zip ZenUI appspec.yml || true
  - mkdir -p "dpl_cd_upload"
  - mv ZenUI.zip dpl_cd_upload/ZenUI.zip || true

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    local_dir: dpl_cd_upload
    bucket: zenithmicro-ui
    region: us-west-2
    skip_cleanup: true
    acl: public_read
    detect_encoding: true
    on:
      repo: airavata-courses/TeamZenith
      all_branches: true
  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY # declared in Travis repo settings
    secret_access_key: $AWS_SECRET_KEY
    bucket: zenithmicro-ui
    key: ZenUI.zip
    bundle_type: zip
    application: zenith-Microserice-UI
    deployment_group: prodENV
    region: us-west-2
    on:
      repo: airavata-courses/TeamZenith
      all_branches: true
after_deploy:
  - echo "Performing after deploy procedures..."
